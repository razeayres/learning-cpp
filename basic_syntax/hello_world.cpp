#include <cstdio> // libraries should be included like this

/* the main function
is the entry point
of any program (it is
called by the operating
system) */
int main()
{
    puts("Hello, World!"); // all statements ends in ;
    return 0;
}