#include <cstdio>

// the first argument is an int
// representing the number of cmd args.
// The second arg is an array of
// characters
int main(int argc, char **argv) // char **argv[] means the same thing
{
    puts("Hello, World!"); // this requires cstdio library
    return 0;
}