#include <cstdio>

int main()
{
    int x = 47;
    int y = 5;
    printf("x is %d\n", x);
    printf("y is %d\n", y);

    // ir can be:
    // +, -, /, *, %
    y += x; 
    printf("y is %d\n", y);

    return 0;
}